{
  "name": "mermaid-to-excalidraw-cli",
  "version": "1.0.1",
  "description": "CLI tool to convert Mermaid diagrams to Excalidraw format",
  "type": "module",
  "bin": {
    "mer2excal": "./dist/cli.js"
  },
  "scripts": {
    "build": "npm run embed-font && tsc",
    "prepublishOnly": "npm run build",
    "postinstall": "npx patch-package",
    "bundle": "npm run build && esbuild dist/cli.js --bundle --platform=node --target=node18 --outfile=dist/cli.bundle.js --format=esm",
    "compile": "npm run bundle && bun build dist/cli.bundle.js --compile --outfile mer2excal",
    "compile:all": "npm run bundle && bun build dist/cli.bundle.js --compile --target=bun-darwin-arm64 --outfile mer2excal-darwin-arm64 && bun build dist/cli.bundle.js --compile --target=bun-darwin-x64 --outfile mer2excal-darwin-x64 && bun build dist/cli.bundle.js --compile --target=bun-windows-x64 --outfile mer2excal-windows-x64.exe",
    "embed-font": "node scripts/embed-font.js"
  },
  "dependencies": {
    "@excalidraw/mermaid-to-excalidraw": "^2.1.1",
    "@excalidraw/utils": "^0.1.3-test32",
    "jsdom": "^24.0.0"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^20",
    "esbuild": "^0.28.1",
    "patch-package": "^8.0.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  }
}
